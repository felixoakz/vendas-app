import React from 'react';
import { View, Text } from 'react-native';

const ConfigScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-2xl font-bold">Config Screen</Text>
    </View>
  );
};

export default ConfigScreen;
