import React from 'react';
import { View, Text } from 'react-native';

const OrdersScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-2xl font-bold">Orders Screen</Text>
    </View>
  );
};

export default OrdersScreen;
